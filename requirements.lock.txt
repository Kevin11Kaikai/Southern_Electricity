# Exact versions used to produce the figures, tables and PDFs shipped in this
# repository. Install these if you want tools/verify_reproduction.py to report
# byte-identical (SHA-256) figure reproduction. Newer versions still reproduce
# the numbers, but matplotlib may emit cosmetically different PDF bytes.
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
matplotlib==3.11.1
pyarrow==25.0.1
pypdf==6.18.1
pdfplumber==0.11.10
pypdfium2==5.13.0
Pillow==12.3.0
