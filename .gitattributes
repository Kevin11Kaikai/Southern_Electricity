# Normalize line endings in the repository; check out native on each platform.
* text=auto eol=lf

# Binary artifacts
*.pdf binary
*.png binary

# Notebooks: keep diffs readable, never merge automatically
*.ipynb -merge
